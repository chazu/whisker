[package]
name = "whisker"
version = "0.1.0"
edition = "2024"
publish = false
description = "Proof of concept for a switchable two-line Bash prompt"

[dependencies]
toml = { version = "1.1.5", default-features = false, features = ["parse", "serde"] }
unicode-width = "0.2.2"
